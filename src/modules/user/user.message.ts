const UserMessage: { [key: string]: string } = Object.freeze({
    USER_EXIST: "It is not possible to create a new user with this information!",
    CREATE_USER_SUCCESS: "New user created successfully.",
    FIELD_VALIDATION: "There was an error validating the sent information"
});

export default UserMessage;
